// internal imports
import { NotFoundContainer } from "./not-found.styles"

// component
const NotFound = () => {
  return (
    <NotFoundContainer>
      <h1>Oopsy daisy. That url doesn't exist.</h1>
    </NotFoundContainer>
  )
}

export default NotFound